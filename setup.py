from setuptools import find_packages, setup

setup(
    name="ATurtle",
    packages=find_packages(),
    version="0.1.0",
    description='This is a school project. I made a turtle just with Python, which is nearly 5 times faster.My first Python library',
    author='SchwammPizza',
)